platform: linux

image_resource:
  type: docker-image
  source:
    repository: microsoft/dotnet

inputs:
- name: HelloWorldDotNetCore

run:
  path: sh
  args: ["https://raw.githubusercontent.com/bigangryrobot/buildscripts/master/dotnetcorebuild.v1.sh"]
